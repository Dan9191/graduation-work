server:
  port: ${SERVER_PORT:8094}

spring:
  application:
    name: ${SPRING_APPLICATION_NAME:rag}
  security:
    oauth2:
      resource-server:
        jwt:
          issuer-uri: ${KEYCLOAK:http://localhost:9090/realms/graduation}
  datasource:
    url: ${DATASOURCE_URL:jdbc:postgresql://localhost:5432/rag?currentSchema=rag1}
    username: ${DATASOURCE_NAME:rag_user}
    password: ${DATASOURCE_PASSWORD:rag_pass}
    driver-class-name: org.postgresql.Driver

app:
  rag:
    rabbit:
      exchange: ${RABBIT_EXCHANGE:article.exchange}
      routing-key: ${RABBIT_ROUTING_KEY:article.outbox}
      queue: ${RABBIT_QUEUE:article.outbox.queue}
    gigachat:
      embedding-model: ${EMBEDDING_MODEL:EmbeddingsGigaR}
      embedding-url: ${EMBEDDING_URL:https://gigachat.devices.sberbank.ru/api/v1/embeddings}
      secret-token: ${SECRET_TOKEN:}
      token-url: ${TOKEN_URL:https://ngw.devices.sberbank.ru:9443/api/v2/oauth}
      llm-url: ${LLM_URL:https://gigachat.devices.sberbank.ru/api/v1/chat/completions}
      llm-model: ${LLM_MODEL:GigaChat-2-Max}
    min-similarity: ${MIN_SIMILARITY:0.50}

springdoc:
  api-docs:
    path: /api/v3/api-docs
  swagger-ui:
    path: /api/swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: health,metrics,prometheus,readiness,liveness
  endpoint:
    health:
      show-details: always
  metrics:
    tags:
      application: "mos-backend-service"